`./create.sh udacity network.yml network-parameters.json` to create a stack

`./update.sh udacity network.yml network-parameters.json` to update an existing stack

`./delete.sh udacity` to delete an existing stack